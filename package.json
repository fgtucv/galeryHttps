{
  "name": "my-project",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "homepage": "https://fgtucv.github.io/galeryHttps/",
  "scripts": {
    "start": "parcel ./src/index.html --no-cache",
    "build": "parcel build ./src/*.html --no-cache --public-url /galeryHttps/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "izitoast": "^1.4.0",
    "parcel": "^2.13.3",
    "simplelightbox": "^2.14.3"
  }
}
